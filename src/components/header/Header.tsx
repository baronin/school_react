import { ReactElement } from 'react';
import './Header.scss';

const Header = (): ReactElement => {
	return (
		<header className="header">
			<h1>beetroot</h1>
		</header>
	);
};

export default Header;
